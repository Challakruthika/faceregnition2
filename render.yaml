services:
  - type: web
    name: face-expression-recognition
    env: python:3.9.20
    plan: free
    buildCommand: "pip install numpy==1.21.0 && pip install -r requirements.txt"
    startCommand: "python app.py"

